name: Build secOS
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/vscode/devcontainers/universal:linux

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install required packages
        run: |
          sudo apt-get update
          sudo apt-get install -y apt-utils debootstrap squashfs-tools xorriso \
            isolinux syslinux-efi grub-pc-bin grub-efi-amd64-bin grub-efi-ia32-bin \
            mtools dosfstools wget unzip virtualbox curl

      - name: Set up build environment
        run: |
          export TERM=xterm-256color
          export DEBIAN_FRONTEND=noninteractive
          chmod +x buildeb/buildeb.sh
          # Set up user similar to Codespaces
          sudo useradd -m -s /bin/bash Sechorda
          sudo usermod -aG sudo Sechorda
          echo "Sechorda ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/Sechorda

      - name: Run build script
        env:
          TERM: xterm-256color
          DEBIAN_FRONTEND: noninteractive
          USER: Sechorda
        run: |
          sudo -E -u Sechorda ./buildeb/buildeb.sh

      - name: Install rclone
        run: |
          curl https://rclone.org/install.sh | sudo bash
          
      - name: Configure rclone
        run: |
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf << EOF
          [r2]
          type = s3
          provider = Cloudflare
          access_key_id = ${{ secrets.R2_ACCESS_KEY_ID }}
          secret_access_key = ${{ secrets.R2_SECRET_ACCESS_KEY }}
          endpoint = ${{ secrets.R2_ENDPOINT }}
          acl = private
          EOF
          
      - name: Upload artifacts to Cloudflare R2
        run: |
          # List files for debugging
          ls -la *.iso *.ova || true
          
          # Upload .iso file if it exists
          if [ -f "secOS.iso" ]; then
            rclone copy secOS.iso r2:${{ secrets.R2_BUCKET_NAME }}/
            echo "Uploaded secOS.iso to R2"
          else
            echo "secOS.iso not found"
          fi
          
          # Upload .ova file if it exists
          if [ -f "secOS.ova" ]; then
            rclone copy secOS.ova r2:${{ secrets.R2_BUCKET_NAME }}/
            echo "Uploaded secOS.ova to R2"
          else
            echo "secOS.ova not found"
          fi
